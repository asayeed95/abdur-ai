# Mnemix X-image briefs — @mnemix_official (Signal Noir)

Template: `../higgsfield-prompts/x-image.md`. 16:9 or 1:1, Signal Noir only. Each image attaches to a named post — no decoration.

## X1 — stat-callout: "200–500ms of silence you're already paying for"
**Supports:** the VAD-window latency post (`voice-ai-memory-latency-is-a-dead-argument` framing). Timeline bar in cyan: the VAD window with memory-return marked inside it. Copy: headline + "designed for sub-300ms voice recall" (the locked phrasing — never a measured number). Labels in Geist Mono.

## X2 — terminal-capture: the atomic upsert
**Supports:** pillar-2 concurrency thread, tweet 5. A single dark terminal panel with the real line: `INSERT … ON CONFLICT DO UPDATE … RETURNING entity_id` — cyan syntax accent, nothing else. Source attribution small-type: "PR #253".

## X3 — diagram: two gates on one ledger
**Supports:** the metering-ledger thread (PR #406). Two hairline gates in front of a table icon: `RLS` and `role-aware trigger`, violet + cyan. Caption line: "Two unrelated failure modes have to align before history can be edited."

## X4 — quote-card: "Serial tests lie."
**Supports:** any recall-collapse repost/follow-up. Three words, Manrope, huge, cyan period. Attribution: mnemix.ai.

Pre-flight: locks clean · palette exact · every image names its post before generation.
