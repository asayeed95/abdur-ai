# Mnemix motion briefs (Signal Noir)

Template: `../higgsfield-prompts/motion-loop.md`. NeuralSphere is the default motif; fluid HD motion (the lag scar from the site work is the bar — no choppy rotation). Every loop supports a narrative.

## M1 — NeuralSphere hero loop (site + post backdrop)
**Supports:** canonical positioning ("memory + enrichment layer for AI agents"). 8s slow-rotate, loop with no visible seam, nodes pulsing cyan/violet at irregular intervals (live recall, not decoration). **The mark is captured, never generated:** source = the shipped WebGL component via the 1440p/25fps capture rig (mnemix PR #414); Higgsfield only for non-mark compositing.

## M2 — "recall collapse" explainer clip (15–20s, X/Reels)
**Supports:** pillar-2 concurrency thread. Storyboard: (1) serial dots flow into one entity — clean; (2) burst of dots at the same instant — N ghost entities appear; (3) all but one fade to dark (orphaned memories); (4) the atomic-upsert line stamps in, dots converge to one. Geist Mono labels only; ends on the closer card.

## M3 — "two gates" micro-loop (4s, ambient)
**Supports:** metering-ledger post. A row appended to a ledger passes two hairline gates (RLS, trigger); an UPDATE arrow bounces off both. Subtle-pulse, no text baked in.

Pre-flight: no-visible-seam loop check · no text baked in unless permanent · founder look-approval on M1 before M2/M3 production.
