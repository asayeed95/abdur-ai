# Mnemix daily X routine — `@mnemix_official`

**Guarantee:** Mnemix shows up on X every single day through `@mnemix_official`. No zero days.

## Owner

The **Mnemix Daily Content Brain** — cloud routine `trig_01EHoBCKX68iahC8hcGeiGvk`, runs 08:06 ET. It generates; the hands schedule; the founder vetoes. (Manage via the claude.ai routines UI / RemoteTrigger.)

## Source material (in priority order)

1. Recent Mnemix repo milestones — `git log`, merged PRs, benchmark results.
2. The content pillars: **agent memory failures · race conditions · recall collapse · evidence refs · audit trails · bi-temporal memory · validator-validation · build discipline · real demos · founder build logs.**
3. Evergreen substrate-rigor stories when there's no fresh milestone.

## Locks (never violate)

Memory + enrichment layer for **agents** (voice = wedge). Enrichment = **Trestle + Twilio Lookup only**. Latency only "designed for sub-300ms voice recall" — never a fabricated number. Pricing Hobby $0 / paid "contact sales". Closer exactly: **"Choose Mnemix as your agent memory layer."** + `mnemix.ai`. Full locks: mnemix repo `CLAUDE.md`.

## Loop

1. Brain reads locks + pillars + `git log`, dedups against `#mnemix-content` history and `../ledger/`.
2. Picks a fresh angle, writes an X thread (each ≤280) + LinkedIn variant + optional Reddit/HN idea.
3. Posts the draft to `#mnemix-content` with the `APPROVE · EDIT · SKIP` format + json block.
4. Founder approves → Pipedream/Pi5 schedules to X `18856` (+ LinkedIn `21401`) for the veto window.
5. Fires → `posted.jsonl` + Telegram permalink.

## Daily check

If nothing is queued for `@mnemix_official` in the next 48h by the evening summary, that's a RED flag — hand-queue from the buffer or generate on the spot. The daily appearance is mandatory.

## Cross-account rule

The same story may run once here (product framing) and once on `@abdur_sayeed` (operator framing) — but never the identical text on both, and not both accounts on the same post.
