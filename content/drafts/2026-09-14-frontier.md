# Frontier notes — 2026-09-14

**Scope:** the brief's frontier questions, answered against this worktree (`draft/glm-abdur-ai-content-2026-09-14`, base `49a1b12`) and the repo's own branches. Everything below carries its command-level receipt in HANDOFF.md.

## 1. Drafts older than 30 days: superseded or worth finishing?

**On this worktree / main, `content/posts/_drafts/` holds exactly one piece:**

- **`the-last-fifteen-percent`** (`abdur-ai-launch-postmortem.md`, committed `3e7cbe6` 2026-07-02 — 74 days old). **Verdict: worth finishing, not superseded.** It covers deploy-boundary gates (P-013…P-016) — adjacent to the flagship's verification patterns (P-008…P-012) but a distinct incident class; nothing published since covers it. Build-plan C-1 already reviewed it **READY** (2026-07-22). What stands between it and publish, in order:
  1. It has **no `register:` field** — post-C-10, `tldr:publish` refuses drafts without one, and the build rejects published posts missing it. It's a clean `reported` (receipts block already present).
  2. Its internal link points at `/aitldr/the-night-the-doctrine-failed`; canonical is now `/writing/…` (C-10).
  3. C-1's open nits: frontmatter `date` (02:15) precedes its own fix commits (05:02–05:13); rename `.md` → `.mdx` on publish.
  4. The contested P-014 pattern id (build-plan C-2 note: contested between this draft and the mnemix-learning records) must be reconciled before shipping either claimant.

**The remaining Mistakes TLDR drafts are not on main.** C-11 says "six drafts remain in `_drafts/`" — true of branch `content/mistakes-tldr-batch-1`, not of main, where the published four were promoted out (`0b9f490`) and the rest never landed. That branch's `_drafts/` shows **five** unpublished pieces (`letting-the-agent-fix-its-own-docker-builds`, `seven-real-defects-and-a-freeze`, `the-case-sensitive-todo-that-lied-about-status`, `the-flag-flip-that-recorded-nothing`, `the-routes-that-should-never-have-been-public`). C-2 recorded eight drafts and C-11 published four; the eight-minus-four arithmetic, the five visible, and the "six remain" note do not reconcile — one draft's whereabouts are **NOT VERIFIED** from this checkout. All five are C-2-era (2026-07-22…29), i.e. ~7 weeks old, none superseded by an approved piece (the approved corpus shares their genre but not their incidents). Worth finishing per C-2's own verdict; each still owes the P-014 reconciliation check before publish.

## 2. Calendar slots with no assigned piece

**`content/calendar/` does not exist in this repo** (verified: `ls content/` — the directory set is `applications, brand, distribution, posts, workflows`). The brief's calendar premise belongs to infrastructure this checkout doesn't have. The de facto content queue is the build-plan's Open table; its unassigned content-shaped slots are:

- **C-3** (series surface for Mistakes TLDR) — blocked on cadence, i.e. on publishing the five drafts above. The dependency chain is: reconcile P-014 → publish remaining TLDRs → C-3 becomes real.
- **This session's six essays** — no calendar exists to slot them into; the distribution README proposes one-per-week Tuesdays, but assigning dates is the human publisher's call.
- **W-2 remainder** (www CNAME + social-card validators) — not content, but it is the only open "verify what's live" slot and it gates any claim that the September work is visible to the public.

## 3. Adjacent finding: the routing-rule file AGENTS.md cites is not on main

`AGENTS.md` names `content/posts/_drafts/CONTENT-ROUTING-RULE.md` as the authority for draft routing. On `main`, that path does not exist — the file lives only on `content/mistakes-tldr-batch-1` (verified with `git show main:…` → "does not exist"). The rule's substance survived into `content/posts/README.md`'s rules table, so behavior is covered; the citation is broken. Recommend either merging the routing-rule file to main or repointing AGENTS.md at the README rules table. Not fixed in this session — AGENTS.md edits and cross-branch cherry-picks are both outside this session's lane; logged here and in HANDOFF.md.
