# Frontier notes — 2026-09-14

**Scope:** the brief's frontier questions, answered against this worktree (`draft/glm-abdur-ai-content-2026-09-14`, base `49a1b12`) and the repo's own branches. Everything below carries its command-level receipt in HANDOFF.md.

## 1. Drafts older than 30 days: superseded or worth finishing?

**On this worktree / main, `content/posts/_drafts/` holds exactly one piece:**

- **`the-last-fifteen-percent`** (`abdur-ai-launch-postmortem.md`, committed `3e7cbe6` 2026-07-02 — 74 days old). **Verdict: worth finishing, not superseded.** It covers deploy-boundary gates (P-013…P-016) — adjacent to the flagship's verification patterns (P-008…P-012) but a distinct incident class; nothing published since covers it. Build-plan C-1 already reviewed it **READY** (2026-07-22). **Update 2026-09-15: gates 1–3 below are now closed on branch `draft/glm-abdur-ai-content-2026-09-14`** — renamed to `the-last-fifteen-percent.mdx` (slug-matching), `register: reported` added, `/aitldr` link moved to canonical `/writing`, and the `tldr:` field restored from the batch-branch version (main's copy was an older revision missing it — the tldr C-1 recorded as "added" only ever lived on `content/mistakes-tldr-batch-1`). Original gate list, for the record:
  1. ~~No `register:` field~~ — done 2026-09-15.
  2. ~~Internal link pointed at `/aitldr/…`~~ — done 2026-09-15.
  3. C-1's open nits: frontmatter `date` (02:15) precedes its own fix commits (05:02–05:13) — **still open, human call** (changing a post's date is editorial, not mechanical); ~~rename `.md` → `.mdx`~~ — done 2026-09-15.
  4. The contested P-014 pattern id (build-plan C-2 note: contested between this draft and the mnemix-learning records) must be reconciled before shipping either claimant — **still open, needs the mnemix-learning records (outside this repo)**.

**The remaining Mistakes TLDR drafts are not on main — and the arithmetic is now fully reconciled (2026-09-15).** At the C-11 commit (`0b9f490`), `_drafts/` held exactly six drafts: five unpublished TLDR pieces plus the launch postmortem — "Six drafts remain in `_drafts/`" was literally accurate, and my earlier "one draft NOT VERIFIED" is withdrawn. The five (`letting-the-agent-fix-its-own-docker-builds`, `seven-real-defects-and-a-freeze`, `the-case-sensitive-todo-that-lied-about-status`, `the-flag-flip-that-recorded-nothing`, `the-routes-that-should-never-have-been-public`) trace to C-2's batch commit `0b7a689`. They vanished from main because `3f56e88` — the **rebased** version of the C-10/C-11 work that actually merged (#39) — carries a `_drafts/` tree with only the postmortem; the pre-rebase commit `0b9f490` had all eight files. The five persist on `content/mistakes-tldr-batch-1` (verified by `git ls-tree`). One verification caveat from this pass: the local `main` ref is two months stale (tip `5348125`, 2026-07-23); all tree checks above were re-run against `origin/main` (tip `49a1b12`). All five are C-2-era (2026-07-22…29), i.e. ~7 weeks old, none superseded by an approved piece (the approved corpus shares their genre but not their incidents). Worth finishing per C-2's own verdict; each still owes the P-014 reconciliation check before publish.

## 2. Calendar slots with no assigned piece

**`content/calendar/` does not exist in this repo** (verified: `ls content/` — the directory set is `applications, brand, distribution, posts, workflows`). The brief's calendar premise belongs to infrastructure this checkout doesn't have. The de facto content queue is the build-plan's Open table; its unassigned content-shaped slots are:

- **C-3** (series surface for Mistakes TLDR) — blocked on cadence, i.e. on publishing the five drafts above. The dependency chain is: reconcile P-014 → publish remaining TLDRs → C-3 becomes real.
- **This session's six essays** — no calendar exists to slot them into; the distribution README proposes one-per-week Tuesdays, but assigning dates is the human publisher's call.
- **W-2 remainder** (www CNAME + social-card validators) — not content, but it is the only open "verify what's live" slot and it gates any claim that the September work is visible to the public.

## 3. Adjacent finding: the routing-rule file AGENTS.md cites is not on main

`AGENTS.md` names `content/posts/_drafts/CONTENT-ROUTING-RULE.md` as the authority for draft routing. On `origin/main`, that path does not exist — it was dropped by the same `3f56e88` rebase as the five TLDR drafts; the file persists on `content/mistakes-tldr-batch-1`. The rule's substance survived into `content/posts/README.md`'s rules table, so behavior was covered; the citation was broken. **Resolved 2026-09-15 on branch `draft/glm-abdur-ai-content-2026-09-14`:** restored `CONTENT-ROUTING-RULE.md` verbatim from the batch branch (via `git show`, no checkout), along with `the-last-fifteen-percent.REVIEW.md` — C-1's READY verdict, also dropped by the rebase. The AGENTS.md citation is valid again once this branch merges.
