---
project: dockerfile-ai
channel: x
account: "@abdur_sayeed"
source: "dockerfile-ai commit 66078c1 — fix(obs): /health probes the real DB dependency instead of always reporting 'ok' (#99)"
angle: "the health check that always said ok"
status: draft
---

## X thread

**1/** Found a classic in my own codebase this week: the /health endpoint returned "ok" unconditionally. Not "checked and healthy." Just… "ok". A health check that cannot say no is a decoration. (188)

**2/** The failure mode: DB goes down, orchestrator asks /health, gets "ok", keeps routing traffic to a service that 500s every real request. Monitoring green, users burning. The check was lying in exactly the situation it exists for. (231)

**3/** The fix (dockerfile-ai #99): /health now probes the actual DB dependency and reports what it finds. One query. That's the whole patch. The hard part was noticing that "we have a health check" and "we check health" were different sentences. (241)

**4/** dockerfile.ai exists for this class of bug — environments and infra that look fine and aren't. Verification over vibes, in the build itself. (143)

## Pre-flight
- [ ] char counts re-verified (weighted) at queue time
- [ ] claim check: commit 66078c1 is real and merged (#99) ✓ verified via git log 2026-07-09
- [ ] dedup vs ledger
