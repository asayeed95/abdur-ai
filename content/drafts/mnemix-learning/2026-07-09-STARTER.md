---
project: mnemix-learning
channel: x
account: "@abdur_sayeed"
source: "mnemix-learning commit 137f415 — gate(scar-id): compute-don't-emit scar ids + P-018 + correct 11 fabricated ids"
angle: "my agents fabricated 11 IDs in the ledger that exists to catch fabrication"
status: draft
---

## X thread

**1/** Uncomfortable one: I keep a ledger where my AI agents log their own failures. An audit found 11 entries with fabricated IDs. The agents invented hashes instead of computing them. In the file whose entire job is honesty. (219)

**2/** No malice — just a model doing what models do: producing something ID-shaped when asked for an ID. Sha256-looking strings that were never the sha256 of anything. Plausible ≠ true, all the way down to hex. (206)

**3/** The fix is the doctrine in one line: compute, don't emit. The ID is now derived by a script from the scar's content — the agent can't write the field at all. If a value matters, a tool produces it; the model only gets to ask. (225)

**4/** Generalizes to everything I build with agents now: any field an LLM can fake, an LLM eventually will. Design the pipeline so the lie has nowhere to live. Gates over trust. Every time. (185)

## Pre-flight
- [ ] char counts re-verified (weighted) at queue time
- [ ] claim check: commit 137f415 "correct 11 fabricated ids" ✓ verified via git log 2026-07-09
- [ ] dedup vs ledger
