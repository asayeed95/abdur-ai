# build-plan.md — abdur.ai

The only todo list that counts for this repo. `PROCESS.md` explains the discipline; this file is the live task state. Every row: `status: todo | in-progress | blocked | review | done` + last-touched date. Status flips happen in the same commit as the work that caused them.

This file was (re)written 2026-07-08 during a build-discipline retrofit, cross-checked against the actual repo state (git log, file contents) rather than copied from `CLAUDE.md`'s original handoff prose — some of that prose predates work that has since shipped.

---

## Phase 1 — Finish Wiring & Launch (current)

### Done (verified, not just claimed)

| task-id | what | evidence | status |
|---|---|---|---|
| D-1 | Wire `/api/subscribe` to Resend audiences | `app/api/subscribe/route.ts` calls the Resend API with `RESEND_API_KEY` / per-list audience IDs; commit `7122c7f` | done — 2026-07-08 |
| D-2 | Generate OG cover image for flagship post | `public/blog/the-night-the-doctrine-failed/cover.jpg` exists; commit `fc4c370` | done — 2026-07-08 |
| D-3 | Replace `TODO_X_HANDLE` / `TODO_GITHUB_HANDLE` placeholders | `grep -r TODO_X_HANDLE\|TODO_GITHUB_HANDLE` returns nothing outside this doc's own history; `lib/site.ts` has real handles (`@asayeed95`, `github.com/asayeed95`) | done — 2026-07-08 |
| D-4 | Add the 3 backlog posts | `content/posts/voice-ai-memory-latency-is-a-dead-argument.mdx`, `cross-video-retention-pattern-detection.mdx`, `who-owns-the-architecture-when-ai-writes-the-code.mdx` all present alongside the flagship post; commit "content: publish three backlog posts" | done — 2026-07-08 |
| D-5 | Link Vercel project | `.vercel/project.json` present (`projectName: abdur-ai`) | done — 2026-07-08 (link only — see W-2 for live-deploy verification, which is NOT the same claim) |
| D-6 | Wire a real ESLint config so `npm run lint` runs headlessly | `eslint.config.mjs` added (flat config, `next/core-web-vitals` + `next/typescript`); repo previously had zero eslint config, which made `next lint` block on an interactive TTY prompt — unusable in CI/pre-commit | done — 2026-07-08 |

### Open

| task-id | what | notes | status |
|---|---|---|---|
| W-1 | Persist `/api/ingest/now` and `/api/ingest/ship` to Supabase | Both routes are currently documented stubs (`console.log`, `TODO(claude-code)` comment in `now/route.ts`). Schema is specified in `CLAUDE.md` §3 (`now_state`, `ship_log` tables, `profile_id` column for future multi-tenant). Wire the insert/upsert, call `revalidateTag('now')` / `revalidateTag('ship')`, then point `components/NowPanel.tsx` / `components/ShipLog.tsx` at Supabase with the seed data as empty-table fallback. | todo |
| W-2 | Verify production deploy is actually live | Partially verified 2026-07-22: `curl -sI https://abdur.ai` → HTTP/2 200, `/aitldr` → 200, `dig +short abdur.ai A` → `76.76.21.21` (correct Vercel record). Still open: `www` CNAME returned nothing on `dig +short www.abdur.ai CNAME`, and the three social-card validators from `CLAUDE.md` §7 have not been re-run. Homepage CDN cache `age` ≈ 19 days — consistent with no deploy/content change since the 2026-07-02 posts. | todo — deploy confirmed live; validators + www record remain |
| W-3 | (Optional, Phase 2 — deferred) Real Stripe checkout for the `Library` products | `components/Library.tsx` still seeds static `PRODUCTS` with no `/api/checkout` route. Explicitly optional per `CLAUDE.md` §8 — do not start without confirming it's actually next. | todo, low priority |
| C-1 | Publish-readiness review of the stalled launch postmortem (`the-last-fifteen-percent`, in `_drafts/` since 2026-06-28) | Verdict **READY** — `_drafts/the-last-fifteen-percent.REVIEW.md`: every claim verified against git history (`6ede7d1`, `82c5c59`, `ce3fa9c`, `452a959`); missing `tldr` field added (173w, ≤180). Open nits for the human publisher: frontmatter date (02:15) precedes its own fix commits (05:02–05:13); rename `.md` → `.mdx` on publish. Publishing itself is a human action per the CONTENT-ROUTING-RULE. | done — 2026-07-22 (publish gated on Abdur approval) |
| C-2 | "Mistakes TLDR" series: canonical format + first draft batch from repo-event sources | `content/workflows/mistakes-tldr/FORMAT.md` (format contract, pattern-id registry, selection criteria, 2–3/wk cadence, pre-publish checklist) + 8 drafts in `content/posts/_drafts/` (slugs dated 2026-07-22…29, patterns P-017…P-024, all lint-clean, `tldr` ≤180w, each traced to one repo-event record with `Used by:` flipped). Open: P-014 id contested between the launch draft and `mnemix-learning` records — reconcile before shipping either; P-009 undefined in-repo (do not reassign). Awaiting Abdur review per routing rule. | done — 2026-07-22 (publish gated on Abdur approval) |
| C-3 | Surface the series on the site (series badge/feed treatment, pattern index) | Wiring only — no locked files, no copy rewrites. Start once cadence is real (a few published entries). | todo |
| C-4 | Content-ops Slice 1 (Pair C) — daily harvest→loop→one-yes batch per Mnemix `docs/superpowers/specs/CONTENT-OPS-SYSTEM.md` (RATIFIED 2026-07-22) | `local-brains/`: gates.py + rc1_loop.py + batchlib.py + batch_assemble.py + r-c1.sh/r-c2-send.sh + hands batch layer + installer rewire; 51 offline tests green; acceptance = spec §14 AC-1…14 (dry-run week = AC-10). Arming = founder-run installer; first live release gated on D-7 lane cleanup + D-8 floor-only. | review — 2026-07-22 (code complete on `content/content-ops-slice1`; launchd install + live release founder-gated) |
| C-5 | Codex Cloud + Linear task handoff | Add reproducible Cloud setup/maintenance scripts, a Linear issue-to-brief helper, and a secret-name manifest. Cloud task intake is explicit (issue ID/context), not an unattended Linear poller. | in-progress — 2026-07-23 |
| C-6 | Draft-only artifact registry foundation | `content/workflows/artifact-registry/artifact_registry.py` + 6 offline contract tests. First real artifact: `content/artifacts/drafts/art_78ecb6f05653cabc201a7edd.json`, sourced from the Mnemix stable-destination TLDR; it remains `draft` with no delivery capability. Interoperability with C-4 is future work because the referenced Mnemix CONTENT-OPS-SYSTEM source is absent from this checkout. | review — 2026-07-30 |
| C-7 | Public-claims and beta-CTA truth synchronization | Replace noncanonical "self-driving" / benchmark language across public abdur.ai surfaces with the Mnemix claims authority; route product CTAs to the verified live beta waitlist; add a static and optional live verifier so drift cannot be mistaken for a valid offer. Static checker, typecheck, lint, production build, and read-only live waitlist/signup verification pass; deployment remains intentionally unperformed. **Correction 2026-09-04:** the static checker was NOT passing on this working tree — six findings, three of them source-formatting artifacts of the line-window scan. Closed under C-10; see RETRO. | review — 2026-09-04 (checker re-verified green) |
| C-8 | Artifact control plane — signed, draft-only Cloudflare ingress contract | C-6 creates source-hashed local manifests. C-8 specifies the next service boundary: signed/idempotent D1 draft ingress, direct source verification rules, no review/publish/schedule endpoint, and test gates before a Queue, Cron, or Pipedream activation. The architecture is recorded in `content/workflows/pipedream/README.md`; implementation is next. | in-progress — 2026-07-29 |
| C-9 | Evidence-carrying review-packet compiler | Re-read each C-6 draft’s source before it can become an LLM planning input; emit one idempotent, bounded, draft-only packet with the exact source snapshot, audience value, and `needs_review` claims. Reject stale source bytes, delivery/review advancement, unsupported evidence, secrets, and machine paths. Twelve offline contract tests pass; first real packet `content/artifacts/review-packets/pkt_273b3833db475c65ae60eacb.json` is draft-only and idempotent. | review — 2026-07-30 |
| C-10 | `/writing` canonical post surface + the three-register claim system | Canonical `/writing/[slug]` + `/writing` index + `/writing/rss.xml`. `/aitldr/*` retained and still 200, now emitting `rel=canonical` at `/writing` and rendering through the same `<PostArticle>` so the two surfaces cannot drift. Registers (`reported` / `designed` / `argued`) declared in frontmatter, defined once in `lib/registers.ts`, rendered as a badge + one-line status note, and enforced by `requireRegister()` (fails `npm run build`) plus `check_registers()` in the claims script. Sitemap, `llms.txt`, JSON-LD `@id`s and all internal links moved to `/writing`; `/uses` dropped from the sitemap (no such route — it was advertising a 404). Both gates mutation-tested in both directions. Also closes C-7's actually-red claims gate. | done — 2026-09-04 (not deployed) |
| C-11 | Publish four `reported` Mistakes TLDR drafts | `29-review-rounds-hardened-a-ci-gate-that-nothing-ran`, `the-dashboard-query-rls-wouldnt-let-through`, `the-health-check-that-became-a-retry-storm`, `the-meter-that-counted-cache-hits-as-cash`. All four read end-to-end, all `register: reported` with `receipts:` carrying commit SHAs and PR numbers, promoted via `npm run tldr:publish` with `content-publish-override:` entries logged per CONTENT-ROUTING-RULE.md. Drafts carrying the contested P-014 id (see C-2) deliberately not selected. Two defects fixed in flight: an X handle disagreeing with `lib/site.ts`, and internal `source:` paths in frontmatter. Six drafts remain in `_drafts/`. | done — 2026-09-04 (not deployed) |
| H-2 | Site-wide clock-resolved light theme + /hire v4 | Palette converted to `rgb(var(--c-*) / <alpha-value>)` (`tailwind.config.ts`) with channel triples and a `:root[data-theme="light"]` block (`app/globals.css`) — logged as a `design-token-override:` in `overrides.md`. Dark proven unchanged: 436/436 compiled CSS rules identical with vars resolved; pixel diff 114/2,048,000 px, all inside the `animate-pulse-clay` dot. Theme resolves pre-paint via a blocking inline script in `app/layout.tsx` (never server-rendered — the clock is the visitor's); tri-state `<ThemeToggle />` (Auto → Light → Dark), Auto stored as absence of the key, re-resolves at the next 06:00/18:00 boundary. `HIRE` added to `NAV` site-wide with current-page state. | done — 2026-09-04 (not deployed) |
| H-3 | Port the /hire v4 design reference | `app/hire/page.tsx` rebuilt from `docs/hire-page/hire-v4/hire-v4.html`: hero, résumé sheet, what-I-build, corrections, how-I-work, client delivery, selected systems, Northsun architecture, case study. Paper literals and the three sheet keyframes are route-scoped (no second override). Interactive layer in `components/hire/HireActions.tsx` (download menu, email modal with topic starters, Esc/backdrop/focus handling). Claims gate green: `claims_policy.IDENTITY` verbatim, retired phrase absent, Mnemix appears only with full Memory-Lab attribution. Repo counts re-verified against `origin/main@3155bc5e` (2026-09-03) — 330 test files, 15 workflows, 16 packages, `@mnemix-ai/client` v0.2.2 all confirmed; the unverifiable "144 automations" was dropped. | done — 2026-09-04 (not deployed) |
| H-1 | Publish canonical recruiter-facing résumé assets | Place the current ATS-first Forward Deployed AI résumé and Master Career résumé in `docs/hire/`, with stable public filenames and editable DOCX counterparts. | done — 2026-07-25 |

## Phase 2 — Stripe + Library checkout (deferred)

Not started. See W-3 above — this whole phase is optional per the original handoff (`CLAUDE.md` §8) and should stay untouched until explicitly prioritized.

## Phase 3 — Notion CMS sync for posts (future)

Not started, not currently planned. Posts stay as MDX in `content/posts/` per `CLAUDE.md`'s "Architecture decisions you should NOT change."

---

## Explicitly out of scope for this build-discipline retrofit

Per the retrofit brief: no external market research, no from-zero blueprint, no 7-phase `/spec/*` pipeline (personas/journeys/ERD/api-contract/etc.) — this project doesn't have the data-model/auth/payments surface those exist to gate. If Phase 2 or 3 above actually kicks off, write real specs for *that* work at that time; don't manufacture specs for phases that aren't happening yet.
