# Hermes — abdur-ai (lean context)

**abdur.ai** — Abdur's public founder logbook, portfolio, and Northsun-facing lead surface (Next.js on Vercel). Priority #2 and the default catch-all anchor for unanchored work. Public claims obey claims law.

Work rules (short): Linear issue before any PR (team AGE); never paste secrets (reference Doppler/.env names); public copy obeys claims law (no invented numbers/customers/benchmarks); heavy implementation → hand to Claude Code via `a2a` (`claude-code`, `northsun-mini`); decide & act per the Autonomy Doctrine, escalate only North-Star conflicts / fatal exposure / irreversible outward actions.

## Context discipline

This file is deliberately short; you re-read it every turn. Full repo law is in `AGENTS.md` / `CLAUDE.md` (and `docs/`) — open them ONLY when the task needs that detail, never by default. Do not preload sub-directory AGENTS.md files, skills, or docs; find the one you need with `skills_list`/`skill_view` or a targeted read.
