name: CI
on:
  workflow_dispatch:
  pull_request:
    branches: [main]
  push:
    branches: [main]
permissions:
  contents: read
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  quality:
    name: Quality gate
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false
      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - name: Validate public claims
        run: python3 scripts/check-public-claims.py
      - name: Brand regression check (Northsun / Mnemix dual-brand law)
        run: python3 scripts/check-brand.py
      - name: Check OG share cards
        run: npm run check:og
      - name: Voice profile tests (AGE-1216)
        run: python3 content/voice/tests/test_voice_profiles.py -v
      - run: npm run build
